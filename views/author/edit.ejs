<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Editing Authors Page</title>
</head>
<body>
<%  if (!isAuthenticated){ %>
    <br>
    <div class="alert alert-warning">
    <a href="/login">You must be logged in to edit an author</a>
    </div>
  <%  } else {%>
   <form action="/authors/<%= author.id %>?_method=put" method="post">
    <input name="author[name]" value="<%= author.name %>" autofocus>
    <input name="author[age]" value="<%= author.age %>">
    <input type="submit" value="Save">
  </form>
<% } %>
</body>
</html>